name: Build with KDE Craft

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: windows-2022

    steps:
      - uses: actions/checkout@v4

      - name: Install Python and Git (required by Craft)
        run: |
          choco install python3 git -y
          # 确保 Python 在 PATH
          echo "C:\Python3" >> $env:GITHUB_PATH
          echo "C:\Python3\Scripts" >> $env:GITHUB_PATH

      - name: Install KDE Craft
        shell: powershell
        run: |
          Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
          iex ((new-object net.webclient).DownloadString('https://raw.githubusercontent.com/KDE/craft/master/setup/install_craft.ps1'))

      - name: Build your project with Craft
        shell: cmd
        run: |
          # 进入 Craft 环境并构建你的应用（你需要为项目编写一个蓝图 .craft 文件）
          cd C:\CraftRoot
          craft your-app-name   # 需先为你的项目定义蓝图

      # 后续上传 artifact 步骤...
